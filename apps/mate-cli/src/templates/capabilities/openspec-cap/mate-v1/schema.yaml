name: mate-v1
version: 1.1
description: Mate's area-scoped OpenSpec workflow - proposal → specs → design → tasks
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      Create the proposal document that establishes WHY this change is needed.

      Sections:
      - **Why**: 1-2 sentences on the problem or opportunity. What problem does this solve? Why now?
      - **What Changes**: Bullet list of changes. Be specific about new capabilities, modifications, or removals. Mark breaking changes with **BREAKING**.
      - **Affected Areas**: List one or more exact repository-relative paths in that Working Repository (for example, `apps/web`, `packages/ui`, or `.` for the repository root). Every change MUST name at least one Area; never use `N/A`. Confirm each path exists before recording it.
      - **Capabilities**: Identify which specs will be created or modified:
         - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<capability>/spec.md` using a kebab-case capability name (e.g., `user-auth`, `data-export`). Never place specs under an Area folder. Record the Area as exact-path `area:` metadata.
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behavior changes (not just implementation details). Each needs a delta spec file. Check `openspec/specs/` for existing spec names. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.

      IMPORTANT: The Capabilities section is critical. It creates the contract between proposal and specs phases. Research existing specs before filling this in. Each capability listed here will need a corresponding spec file.

      Keep it concise (1-2 pages). Focus on the "why" not the "how" - implementation details belong in design.md.

      This is the foundation - specs, design, and tasks all build on this.

      Area scope rules:
      - Every Area MUST be an exact repository-relative path.
      - Area identity is metadata, never part of a change or capability folder name.
      - Every delta and canonical spec MUST include `area: <area>` in its YAML frontmatter.

      **Obsidian**: Emit this YAML frontmatter at the very top of the file. Replace `<change-name>` with the actual change folder name (e.g. `my-change`):

      ```
      ---
      type: change-proposal
      change: <change-name>
      schema: mate-v1
      status: active
      tags: [openspec/change, openspec/proposal]
      ---
      ```

      At the end of the file, add a `## References` section with `[[wiki-links]]` to each spec listed in the Capabilities section:

      ```
      ## References

      - [[specs/my-capability/spec]]
      ```
    requires: []
  - id: specs
    generates: specs/**/*.md
    description: Detailed specifications for the change
    template: spec.md
    instruction: |
      Create specification files that define WHAT the system should do.

      Create one spec file per capability listed in the proposal's Capabilities section.

      **Path and scope rules** — spec files are ALWAYS flat; Area is required metadata, never a folder:
      - Every spec lives at `specs/<capability>/spec.md`. Never interpose an Area folder. `specs/<area>/<capability>/spec.md` breaks the OpenSpec CLI: it parses spec deltas only at the flat path, so an Area folder makes `openspec show`/`validate` report zero deltas.
      - Every delta and canonical spec MUST record its Area in the `area:` frontmatter field as an exact repository-relative path, such as `apps/web`, `packages/ui`, or `.`. Do not convert paths to `app_*` or `pkg_*` tokens.
      - The `area:` field is mandatory even when every requirement in the file belongs to the same Area.
      - Every requirement MUST also include an inline `**Area:**` marker with one or more exact repository-relative paths. This is required even when it repeats the spec's `area:` metadata.
      - Modified capabilities: match the existing `specs/<capability>/spec.md` path exactly.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed behavior - MUST include full updated content
      - **REMOVED Requirements**: Deprecated features - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Every requirement MUST include `**Area:** \`<area>\`` after its SHALL/MUST statement and before its first scenario. For a REMOVED requirement without a normative statement, put it after `**Migration**`. Use comma-separated backticked paths when it affects multiple Areas.
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.

      MODIFIED requirements workflow:
      1. Locate the existing requirement in `openspec/specs/<capability>/spec.md`.
      2. Copy the ENTIRE requirement block (from `### Requirement:` through all scenarios)
      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior
      4. Ensure header text matches exactly (whitespace-insensitive)

      Common pitfall: Using MODIFIED with partial content loses detail at archive time. If adding new concerns without changing existing behavior, use ADDED instead.

      Example:

      ```
      ## ADDED Requirements

      ### Requirement: User can export data
      The system SHALL allow users to export their data in CSV format.
      **Area:** `apps/web`

      #### Scenario: Successful export
      - **WHEN** user clicks "Export" button
      - **THEN** system downloads a CSV file with all user data

      ## REMOVED Requirements

      ### Requirement: Legacy export
      **Reason**: Replaced by new export system
      **Migration**: Use new export endpoint at /api/v2/export
      **Area:** `apps/web`
      ```

      Specs should be testable - each scenario is a potential test case.

      **Obsidian**: Emit this YAML frontmatter at the very top of each spec file. Replace `<change-name>`, `<area>`, and `<capability>` with actual values. `area` is mandatory and MUST be the exact repository-relative path that owns the capability:

      ```
      ---
      type: delta-spec
      change: <change-name>
      area: <area>
      capability: <capability>
      tags: [openspec/change, openspec/spec, openspec/delta]
      ---
      ```

      After the file's `#` title line, add a backlink on the next line: `← [[proposal]]`. If modifying an existing main spec, add on the line after: `Applies to: [[openspec/specs/<capability>/spec]]`.

      Migration rules for existing artifacts and canonical specs:
      1. Convert legacy `N/A` Areas to the explicit repository-relative Area `.` only when the repository root is the intended Area; otherwise inspect the repository and choose the real path.
      2. Convert legacy `app_*`, `pkg_*`, and similar synthetic Area tokens to their exact repository-relative paths.
      3. Add mandatory `area:` frontmatter to every delta and canonical spec that lacks it.
      4. Add the mandatory inline `**Area:**` marker to every requirement that lacks it.
      5. Preserve Area metadata and inline markers when archiving, validating, displaying, or finishing artifacts.
    requires:
      - proposal
  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: |
      Create the design document that explains HOW to implement the change.

      When to include design.md (create only if any apply):
      - Cross-cutting change (multiple services/modules) or new architectural pattern
      - New external dependency or significant data model changes
      - Security, performance, or migration complexity
      - Ambiguity that benefits from technical decisions before coding

      Sections:
      - **Context**: Background, current state, constraints, stakeholders
      - **Goals / Non-Goals**: What this design achieves and explicitly excludes
      - **Decisions**: Key technical choices with rationale (why X over Y?). Include alternatives considered for each decision.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)
      - **Open Questions**: Outstanding decisions or unknowns to resolve

      Focus on architecture and approach, not line-by-line implementation. Reference the proposal for motivation and specs for requirements.

      Good design docs explain the "why" behind technical decisions.

      **Obsidian**: Emit this YAML frontmatter at the very top of the file. Replace `<change-name>` with the actual change folder name:

      ```
      ---
      type: change-design
      change: <change-name>
      schema: mate-v1
      tags: [openspec/change, openspec/design]
      ---
      ```

      After the file's `#` title, add on the next line: `← [[proposal]]`. Reference relevant spec files inline using `[[specs/<capability>/spec]]` where appropriate.
    requires:
      - proposal
  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: |
      Create the task list that breaks down the implementation work.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses checkbox format to track progress. Tasks not using `- [ ]` won't be tracked.

      Guidelines:
      - Group tasks by the exact Areas listed in the proposal, then by concern within each Area. Use `## <area>` as the top-level heading (for example, `## apps/web` or `## .`) with numbered sub-sections beneath it.
      - Each task MUST be a checkbox: `- [ ] X.Y Task description`
      - Tasks should be small enough to complete in one session
      - Order tasks by dependency (what must be done first?)

      Example:

      ```
      ## 1. Setup

      - [ ] 1.1 Create new module structure
      - [ ] 1.2 Add dependencies to package.json

      ## 2. Core Implementation

      - [ ] 2.1 Implement data export function
      - [ ] 2.2 Add CSV formatting utilities
      ```

      Reference specs for what needs to be built, design for how to build it. Each task should be verifiable - you know when it's done.

      **Skill tagging**: Tag each task with `[skill:<name>]` at the end of its checkbox line when a skill applies.

      No-tag cases — implement these directly with no skill tag:
      - Config file changes (`vite.config.ts`, `next.config.ts`, etc.)
      - Constant or route definition additions
      - Package exports / index wiring
      - Single-file edits that follow no special convention
      - Rebuilding or compiling packages
      - Running checks / type checks / lint
      - Trivial utility functions co-located in a component or hook

      **Obsidian**: Emit this YAML frontmatter at the very top of the file. Replace `<change-name>` with the actual change folder name:

      ```
      ---
      type: change-tasks
      change: <change-name>
      schema: mate-v1
      tags: [openspec/change, openspec/tasks]
      ---
      ```

      After the file's `#` title, add on the next line: `← [[proposal]] | [[design]]`. At the end of the file, add a `## References` section with `[[wiki-links]]` for each affected spec area:

      ```
      ## References

      - [[specs/my-capability/spec]]
      ```
    requires:
      - specs
      - design
apply:
  requires:
    - tasks
  tracks: tasks.md
  instruction: |
    Read context files and work through pending tasks only within the proposal's affected Areas. Mark complete as you go.
    Pause if you hit blockers or need clarification.
